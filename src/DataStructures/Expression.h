/*#ifndef EXPRESSION_H
#define EXPRESSION_H

#include "Statement.h"

class Expression : public Statement{

public:
	//Expression(vector<Statement*>* statements_in);
	//Expression(Statement* single_statement_in);
	virtual ~Expression();

	void pretty_print(const int& indent) const;
private:
	/* 
	NOTE: All vector pointers point to dynamically allocated vectors. In case of vector of Statement* 
	the objects in the vector are dynamically allocated as well


};



#endif
*/
