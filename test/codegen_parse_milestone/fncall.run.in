# 2 "arithmetic.run.in" 3 4
int x()
{}


int g, b, d, f=a;


int zz(int a, int b, int c)
{
  int a, b=a+b, c=(a-A)*(a+a) ;
  a=~b*zz(5+10, 40, d);

  a=zz(b, zz(c,f,g), 80*90);

  if(a==b)
    a=b;
  else if(a/c*d) ;
  else if(a==d) {

  }
  else{
    int fsdfsdfs;
    return a==b;
  }

	while(a==b){
		a=b;
	}

	for(i=a; i<a; i=b){
		a=b;
	}  
	return a==b;
}